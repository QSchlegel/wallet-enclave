# DECISIONS — Wallet Enclave

## 2026-02-06
- Node/TS package scaffold created.
- Local-only bind (127.0.0.1) for MVP.
- Policy defaults: allowlist + per-tx/per-day caps + fee caps + idempotency_key + audit log.
